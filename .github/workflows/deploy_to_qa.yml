name: Deploy Develop to QA

on:
  workflow_dispatch: 
  pull_request:
    branches:
      - qa
  push:
    branches:
      - qa

jobs:
  deploy_qa:
    runs-on: windows-latest
    steps:
      - name: Checkout código fuente
        uses: actions/checkout@v4

      - name: Integrando entorno QA
        shell: pwsh
        run: |
          Write-Output "Integrando código para QA"

      - name: Integrando entorno en QA
        shell: pwsh
        run: |
          Write-Output "Integrando código desde Develop en QA"
